# runbook_howto -- procedural how-to questions. Should retrieve a runbook
# or platform doc and surface concrete steps/commands.
#
# These leverage the SRE knowledge base content shipped on 2026-05-02
# (49 platform runbooks + 8 service-scoped + cleaned alerts: frontmatter).

- id: howto_001
  category: runbook_howto
  query: "How do I enable a new GitLab CI pipeline using the templates?"
  expected_sources:
    - devops/gitops-pipeline-templates/README.md
  must_contain:
    - ".gitlab-ci.yml"
    - "include"
  must_not_contain:
    - "I don't have access"
  notes: "Setup procedure from README -- already strong baseline answer."

- id: howto_002
  category: runbook_howto
  query: "When the Celery worker is offline alert fires for acme-notes-be, what should I check first?"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/runbooks/celery/the-celery-worker-is-offline.md
  must_contain:
    - "celery"
  must_not_contain:
    - "context does not contain"
    - "I cannot determine"
  notes: "Tests alert->runbook routing for an alert with populated alerts: frontmatter."

- id: howto_003
  category: runbook_howto
  query: "How do I diagnose Elasticsearch unassigned shards?"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/runbooks/elasticsearch/elasticsearch-has-unassigned-shards.md
  must_contain:
    - "kubectl"
    - "shard"
  must_not_contain:
    - "no procedure"
  notes: "Procedural runbook with concrete commands -- Pass 1 cleanup populated this one."

- id: howto_004
  category: runbook_howto
  query: "What is the procedure when ArgoCD UI is down?"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/runbooks/argocd/argocd-ui-is-down.md
  must_contain:
    - "argocd"
  must_not_contain:
    - "context does not contain"
  notes: "ArgoCD runbook hit -- alerts: was empty pre-Pass-A."

- id: howto_005
  category: runbook_howto
  query: "When a Kafka topic has consumer lag, what diagnosis steps should I take?"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/services/acme-notes-be/runbooks/kafka-consumer-lag-large.md
    - devops/sre/sre-knowledge-base/docs/runbooks/kafka/kafka-topic-has-consumer-lag.md
  must_contain:
    - "consumer"
    - "lag"
  must_not_contain:
    - "no information"
  notes: "Cross-doc -- should pull both acme-notes-be-scoped runbook AND platform-wide kafka runbook."

- id: howto_006
  category: runbook_howto
  query: "How do I respond to an application pod stuck in CrashLoopBackOff?"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/runbooks/kubernetes/application-pod-is-under-crashloopbackoff.md
  must_contain:
    - "kubectl"
  must_not_contain:
    - "context does not contain"
  notes: "Smoke-tested as a known weak retrieval -- captures pre-Step-1 baseline so we can measure parent-substitution improvement."

# -- Tuning regression suite (Tasks 10-13, 2026-05-05) --
# Locks the fixes for the failure modes from sessions
# 558b9c78 / 7a2ec313 / 756c0aa5 + the gateway-lens scenario.

- id: howto_007
  category: runbook_howto
  query: "I want to add new environment variable API_MAPPING_NAME = \"testingblablabla\" to acme-notes-be in PROD environment. And I want to add new secret as environment variable API_MAPPING_KEY = acme-notes-be-mapping-key to acme-notes-be in PROD environment. Pls guide me"
  expected_sources:
    - devops/gitops/values/saas/acme-notes-be/prod.yaml
    - devops/sre/sre-knowledge-base/docs/runbooks/deployments/onboarding-new-saas-app.md
  must_contain:
    - "values/saas/acme-notes-be/prod.yaml"
    - "externalSecretStore"
    - "API_MAPPING_KEY"
    - "remoteRef"
  must_not_contain:
    - "context does not contain"
    - "Missing Information"
    - "I cannot determine"
  notes: |
    Regression for session 558b9c78: pre-fix answer said "context does not
    contain specific instructions" and listed what was missing. After
    Tasks 10+11+12 fixes (repo-detect demote + path-pattern fan-out + gen
    prompt rewrite), retrieval surfaces gitops-monorepo's
    values/saas/acme-notes-be/prod.yaml and the LLM produces concrete YAML
    edits (environment: + externalSecretStore: blocks).

- id: howto_008
  category: runbook_howto
  query: "qa-runner has new service name 'gateway-lens', pls guide me how to onboard it"
  expected_sources:
    - devops/sre/sre-knowledge-base/docs/runbooks/deployments/onboarding-new-saas-app.md
  must_contain:
    - "values/saas/gateway-lens"
    - "gateway-lens"
  must_not_contain:
    - "context does not contain"
    - "Missing Information"
  notes: |
    Regression for the 2026-05-02 gateway-lens scenario. Pre-fix answer
    advised editing values/saas/acme-notes-be/preprod.yaml's existing paths block
    (because retrieval pulled acme-notes-be/preprod.yaml as top source). Post-fix,
    the onboarding runbook surfaces with concrete worked examples
    (gateway-lens, acme-qc-be, mobile-slack) and the answer
    correctly says to create a NEW folder under values/saas/gateway-lens/.
